# render.yaml
services:
  - type: web
    name: health-app-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && gunicorn app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false  # This indicates it's a secret value that should be set in Render dashboard
